export async function db():Promise<IDBDatabase>{return new Promise((resolve,reject)=>{const req=indexedDB.open('syncwrite-workspace',1);req.onupgradeneeded=()=>req.result.createObjectStore('metadata');req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
export async function loadDocs():Promise<any[]>{const d=await db();return new Promise((res,rej)=>{const r=d.transaction('metadata').objectStore('metadata').get('documents');r.onsuccess=()=>{res(r.result||[]);d.close()};r.onerror=()=>rej(r.error)})}
export async function saveDocs(docs:any[]){const d=await db();return new Promise<void>((res,rej)=>{const tx=d.transaction('metadata','readwrite');tx.objectStore('metadata').put(docs,'documents');tx.oncomplete=()=>{d.close();res()};tx.onerror=()=>rej(tx.error)})}
